<h1>New Beer</h1>

<%= form_for(@beer) do |f| %>
  <%= f.select :brewery_id, options_from_collection_for_select(@breweries, :id, :name) %>
  nimi <%= f.text_field :name %>
  tyyli: <%= f.select :style, options_for_select(@styles) %>

  <%= f.submit %>
<% end %>


<%= link_to 'Back', beers_path %>
